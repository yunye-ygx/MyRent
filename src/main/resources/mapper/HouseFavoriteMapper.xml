<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yy.myrent.mapper.HouseFavoriteMapper">

    <select id="selectFavoriteAggRows" resultType="cn.yy.myrent.service.hot.HouseFavoriteAggRow">
        select house_id as houseId,
               count(*) as totalFavoriteCount,
               sum(case when favorite_time &gt;= #{recentSince} then 1 else 0 end) as recentFavoriteCount
        from house_favorite
        where status = 1
        group by house_id
    </select>
</mapper>
