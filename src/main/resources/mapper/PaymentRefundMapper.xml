<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yy.myrent.mapper.PaymentRefundMapper">

    <select id="selectByRequestNo" resultType="cn.yy.myrent.entity.PaymentRefund" parameterType="java.lang.String">
        select *
        from payment_refund
        where request_no = #{requestNo}
        limit 1
    </select>

    <select id="selectByUserIdAndOrderNos" resultType="cn.yy.myrent.entity.PaymentRefund">
        select *
        from payment_refund
        where user_id = #{userId}
          and order_no in
        <foreach collection="orderNos" item="orderNo" open="(" separator="," close=")">
            #{orderNo}
        </foreach>
        order by id desc
    </select>

    <select id="selectPendingForProcess" resultType="cn.yy.myrent.entity.PaymentRefund">
        select *
        from payment_refund
        where status in (0, 3)
          and (next_retry_time is null or next_retry_time &lt;= #{now})
        order by id asc
        limit #{limit}
    </select>
</mapper>
